/**
 * The board. Practice against bots, or a live match against people.
 *
 * The two modes share every rule and every pixel of the renderer, and differ in
 * exactly one thing: who owns the clock. Practice steps a local engine; live
 * polls the server and steps nothing at all.
 */

export { default } from "../SoloBoard.tsx";
